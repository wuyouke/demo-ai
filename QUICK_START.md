# ⚡ 快速开始指南

## 1️⃣ 启动应用

```bash
# 进入项目目录
cd /Users/wuyouke/learning_ai/demo-ai

# 编译和打包
mvn clean package -DskipTests

# 启动应用
java -jar target/demo-ai-*.jar
```

**预期输出**：
```
2026-03-25 XX:XX:XX.XXX  INFO .... Application started successfully
```

## 2️⃣ 访问任务规划页面

在浏览器中访问：
```
http://localhost:8888/task-planning.html
```

## 3️⃣ 使用示例

### 示例1：简单天气查询

1. 在"输入您的目标"框中输入：
```
查询北京今天的天气情况
```

2. 点击🚀 "规划任务"按钮

3. 等待AI分解任务（2-3秒）

4. 查看规划结果，点击▶️ "执行任务"

### 示例2：复杂多步骤任务

1. 输入目标：
```
帮我查询北京明天的天气，并推荐该地区的旅游景点，景点类型为自然景观
```

2. （可选）输入上下文：
```
地点: 北京
用户偏好: 自然景观
```

3. 规划并执行

### 示例3：多工具调用

1. 输入目标：
```
分析一段视频，提取关键帧，生成摘要
```

2. 观察系统如何分解为多个子任务

## 4️⃣ API 快速参考

### 规划任务
```bash
curl -X POST http://localhost:8888/api/tasks/plan \
  -H "Content-Type: application/json" \
  -d '{
    "goal": "查询天气",
    "context": { "location": "北京" }
  }'
```

### 执行任务
```bash
curl -X POST http://localhost:8888/api/tasks/execute \
  -H "Content-Type: application/json" \
  -d '{
    "tasks": [...],
    "sequential": false
  }'
```

### 验证依赖关系
```bash
curl -X POST http://localhost:8888/api/tasks/validate \
  -H "Content-Type: application/json" \
  -d '{
    "tasks": [...]
  }'
```

## 5️⃣ 功能演示

### 前端交互流程

```
┌─────────────────────────────────────┐
│  1️⃣  输入目标和上下文                │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│  2️⃣  点击"规划任务"按钮              │
│     - 显示加载状态                   │
│     - 调用 /api/tasks/plan          │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│  3️⃣  查看规划结果                    │
│     - 任务名称、描述                 │
│     - 任务类型、优先级               │
│     - 依赖关系                       │
│     - 格式化的规划文本               │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│  4️⃣  点击"执行任务"按钮              │
│     - 验证依赖关系                   │
│     - 提交执行请求                   │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│  5️⃣  监控任务执行                    │
│     - 实时更新任务状态               │
│     - 显示执行时间                   │
│     - 更新进度条                     │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│  6️⃣  查看执行结果                    │
│     - 任务执行统计                   │
│     - 成功/失败任务数                │
│     - 总耗时                         │
│     - 任务详情                       │
└─────────────────────────────────────┘
```

## 6️⃣ 前端界面说明

### 左侧：任务规划面板
- **目标输入框**：输入您想完成的目标
- **上下文输入框**：输入背景信息（可选）
- **规划任务按钮**：触发AI规划
- **清空按钮**：清空所有输入

### 右侧：执行监控面板

#### 任务列表标签
- 每个任务显示为卡片
- 显示任务编号、名称、状态
- 显示任务类型、工具、优先级
- 显示执行耗时（如果已完成）

#### 执行统计标签
- **总任务数**：要执行的任务总数
- **已完成**：已经完成的任务数
- **失败**：执行失败的任务数
- **成功率**：完成率百分比
- **进度条**：可视化显示完成进度

### 底部：按钮功能
- **清空任务**：清空任务列表和统计
- **导出结果**：将结果保存为 JSON 文件

## 7️⃣ 常见错误排查

### 错误：网络错误，请稍后重试

**可能原因**：
- 后端服务未启动
- 端口号不正确
- 防火墙阻止

**解决方案**：
```bash
# 检查服务是否运行
lsof -i :8888

# 重启服务
java -jar target/demo-ai-*.jar
```

### 错误：任务规划失败

**可能原因**：
- AI API 调用失败
- API Key 无效
- 网络超时

**解决方案**：
- 检查 `application.yml` 中的 API Key
- 查看后端日志获取详细错误信息

### 错误：任务执行失败

**可能原因**：
- 所需的工具未注册
- 工具执行错误
- 依赖任务失败

**解决方案**：
- 检查工具是否已注册
- 查看任务错误消息
- 检查任务依赖关系

## 8️⃣ 日志查看

### 查看实时日志
```bash
# 查看应用日志
tail -f logs/app.log

# 查看特定错误
grep ERROR logs/app.log

# 查看任务规划日志
grep TaskPlanner logs/app.log

# 查看任务执行日志
grep TaskExecutor logs/app.log
```

## 9️⃣ 性能调优

### 调整并发数
编辑 `TaskPlanningController.java`：
```java
// 创建执行器时指定并发数
TaskExecutor executor = new TaskExecutor(toolRegistry, 5); // 改为5
```

### 调整 AI 模型参数
编辑 `ConversationService.java`：
```java
// 调整 temperature（创意度）
.temperature(0.5) // 默认0.3，0-1范围，越高越有创意
```

### 优化规划提示词
编辑 `TaskPlanner.java` 中的 `buildPlanningPrompt()` 方法

## 🔟 下一步

- [ ] 查看 [完整使用指南](TASK_PLANNING_GUIDE.md)
- [ ] 查看 [实现总结](IMPLEMENTATION_SUMMARY.md)
- [ ] 集成到您的应用中
- [ ] 自定义工具和规划逻辑
- [ ] 部署到生产环境

## 📞 获取帮助

遇到问题？

1. **查看日志** - 最有用的信息来源
2. **查看文档** - 详细的功能说明
3. **检查配置** - 确保 API Key 等配置正确
4. **单步调试** - 使用 IDE 的调试功能

---

**准备好了吗？现在就开始体验智能任务规划系统吧！** 🚀

